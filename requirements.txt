# The real dependency list lives in src/requirements.txt, because
# `adk deploy cloud_run ./src` only installs a requirements.txt found inside
# the agent directory. Keeping one canonical file there and pointing at it
# from here stops the two from drifting apart.
-r src/requirements.txt
