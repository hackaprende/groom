[pytest]
# tests/ is on the path so conftest helpers import directly; the project root is
# there so `src` resolves. No network, no cloud calls: the suite must never be
# the thing that spends money on Gemini.
pythonpath = . tests
testpaths = tests
addopts = -q
